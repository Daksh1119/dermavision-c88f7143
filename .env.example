# Copy this file to ".env" locally and fill in the placeholders.
# Never commit real secrets to the repository.

# -----------------------------
# Supabase configuration
# -----------------------------
# Your Supabase project ref (the subdomain before .supabase.co)
# Example: if your URL is https://fvscthoxggukgjutelcmz.supabase.co
# then PROJECT_ID is fvscthoxggukgjutelcmz
VITE_SUPABASE_PROJECT_ID="YOUR_SUPABASE_PROJECT_ID"

# Full Supabase URL (must match the same project ref as above)
VITE_SUPABASE_URL="https://YOUR_SUPABASE_PROJECT_ID.supabase.co"

# Public anon/publishable key for THIS project (from Supabase > Settings > API)
# Use the exact key for the same project as VITE_SUPABASE_URL.
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Optional: some codepaths may also read this variable.
# If you keep it, set it to the same exact value as VITE_SUPABASE_ANON_KEY.
VITE_SUPABASE_PUBLISHABLE_KEY="YOUR_SUPABASE_ANON_KEY"

# Storage bucket for profile avatars (non-secret; keep default unless you renamed it)
VITE_SUPABASE_AVATAR_BUCKET="avatars"

# -----------------------------
# Model routing
# -----------------------------
# true  = use Supabase Edge Function path (AILabTools)
# false = call your local ML API at VITE_API_URL
VITE_USE_AILABTOOLS=true

# Only set this when VITE_USE_AILABTOOLS=false and you run a local API.
# VITE_API_URL="http://localhost:8080"

# -----------------------------
# External APIs
# -----------------------------
# Google Maps/Places API key (use a restricted key; HTTP referrer + API restrictions)
VITE_GOOGLE_PLACES_API_KEY="YOUR_GOOGLE_PLACES_API_KEY"

VITE_SITE_URL="https://YOUR-PRODUCTION-DOMAIN"